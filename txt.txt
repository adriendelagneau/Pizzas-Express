<table>
   
    <?php foreach($titi as $allArticle) { ?>
     <tr>
        <td><?= htmlspecialchars($allArticle["pizzasName"]) ?></td>
       
         <td><?= htmlspecialchars($allArticle["prixLarge"]) ?> $</td>
        <td><?= htmlspecialchars($allArticle["prixMoyenne"]) ?> $</td>
 </tr>
 <tr>
        <td><?= htmlspecialchars($allArticle["ingredients"]) ?></td>
        </tr>
 <?php } ?>
    
</table>





 <tr>
        <td><?= $allArticle["pizzasName"] ?></td>
       
         <td><? $allArticle["prixLarge"] ?> $</td>
        <td><?= $allArticle["prixMoyenne"] ?> $</td>
    </tr>
    <tr>
        <td><?= $allArticle["ingredients"] ?></td>
    </tr>
    
pensez au sitmap
pensez seo

/*validation du formulaire*/ 

document.getElementById("inscription").addEventListener("submit", function (e) {
    e.preventDefault();
    let erreur;
    var inputs = this.getElementsByTagName("input");

    for (let i = 0; i < inputs.length; i++) {
        if (!inputs[i].value) {
            erreur = "Veillez renseigner tout les champs";
            
        }
    }
    if (erreur) {
        e.preventDefault();
        document.getElementById("erreur").innerHTML = erreur;
        
    } else {
        window.alert('Formulaire envoyé');
    }

})